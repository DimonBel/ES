# Полная документация системы контроля доступа (Electronic Lock System)

**Версия:** 1.0  
**Платформа:** Arduino Uno  
**Дата:** February 2026

---

## Содержание

1. [Обзор системы](#обзор-системы)
2. [Архитектура приложения](#архитектура-приложения)
3. [Аппаратная конфигурация](#аппаратная-конфигурация)
4. [Описание модулей](#описание-модулей)
5. [Последовательность работы](#последовательность-работы)
6. [Схема подключения](#схема-подключения)
7. [Компиляция и загрузка](#компиляция-и-загрузка)
8. [Диагностика и отладка](#диагностика-и-отладка)

---

## Обзор системы

**Electronic Lock System** — это система контроля доступа на базе Arduino, которая позволяет:

- 🔐 Вводить 4-значный цифровой код через матричную клавиатуру 4×4
- 📺 Отображать информацию на LCD дисплее 16×2 (I2C)
- 💡 Визуализировать состояния с помощью цветных LED (зелёный/красный/программирование)
- 🔧 Динамически изменять пароль через режим программирования
- 🖥️ Выводить отладочную информацию через Serial порт

### Основные возможности

- **Проверка пароля:** Пользователь вводит код и система проверяет его
- **Режим программирования:** Возможность установить новый пароль (вход через клавишу 'D')
- **Визуальная индикация:** 
  - Зелёный LED = доступ разрешён
  - Красный LED = доступ запрещён (мигание)
  - LED программирования = активен режим изменения пароля
- **Интерактивный интерфейс:** LCD показывает текущее состояние и запросы
- **Отладка:** Вся информация логируется через Serial (9600 baud)

---

## Архитектура приложения

### Модульная структура

```
ES (Electronic Lock System)
│
├── src/
│   ├── main.cpp                    # Точка входа, инициализация, главный цикл
│   │
│   └── modules/
│       ├── app/                    # Логика приложения (контроллер)
│       │   ├── app.h
│       │   └── app.cpp
│       │
│       ├── keypad/                 # Управление клавиатурой 4×4
│       │   ├── keypad.h
│       │   └── keypad.cpp
│       │
│       ├── lcd/                    # Управление LCD дисплеем (I2C)
│       │   ├── lcd.h
│       │   └── lcd.cpp
│       │
│       ├── led/                    # Управление светодиодами
│       │   ├── led.h
│       │   └── led.cpp
│       │
│       ├── serial_stdio/           # Serial ввод/вывод (stdio)
│       │   ├── serial_stdio.h
│       │   └── serial_stdio.cpp
│       │
│       ├── command/                # Парсер команд (резерв)
│       │   ├── command.h
│       │   └── command.cpp
│       │
│       └── utils/                  # Утилиты
│           └── i2c_scanner.h       # Сканер I2C устройств
│
├── platformio.ini                  # Конфигурация проекта
└── README.md
```

### Диаграмма зависимостей модулей

```
main.cpp
    │
    ├──> App (app.cpp)
    │      ├──> Keypad (keypad.cpp)
    │      ├──> LcdI2c (lcd.cpp)
    │      │       └──> LiquidCrystal_I2C [библиотека]
    │      └──> Led (led.cpp) × 3
    │
    ├──> SerialStdio (serial_stdio.cpp)
    │
    └──> I2CScanner (i2c_scanner.h)
```

### Паттерны проектирования

1. **Модульность:** Каждый компонент (Keypad, LCD, LED) — отдельный класс
2. **Инкапсуляция:** Детали реализации скрыты в приватных методах
3. **Композиция:** Класс `App` агрегирует все компоненты
4. **Singleton-подобный подход:** Глобальные объекты в `main.cpp` для управления ресурсами
5. **RAII (Resource Acquisition Is Initialization):** Инициализация в конструкторах, освобождение автоматическое

---

## Аппаратная конфигурация

### Используемые компоненты

| Компонент | Модель | Интерфейс | Назначение |
|-----------|--------|-----------|------------|
| Микроконтроллер | Arduino Uno | — | Основная плата |
| Клавиатура | Матричная 4×4 | GPIO (8 пинов) | Ввод кода |
| Дисплей | LCD 16×2 | I2C (0x27) | Отображение информации |
| LED зелёный | Стандартный 5mm | GPIO | Индикация успеха |
| LED красный | Стандартный 5mm | GPIO | Индикация ошибки |
| LED программирования | Стандартный 5mm | GPIO | Режим программирования |

### Распиновка Arduino Uno

#### Клавиатура 4×4 (8 пинов)

| Назначение | Arduino Pin | Описание |
|------------|-------------|----------|
| **Строки (Rows)** | | |
| R1 | 8 | Первый ряд клавиатуры |
| R2 | 9 | Второй ряд клавиатуры |
| R3 | 10 | Третий ряд клавиатуры |
| R4 | 11 | Четвёртый ряд клавиатуры |
| **Столбцы (Columns)** | | |
| C1 | 4 | Первый столбец |
| C2 | 5 | Второй столбец |
| C3 | 6 | Третий столбец |
| C4 | 7 | Четвёртый столбец |

**Раскладка клавиатуры:**
```
┌───┬───┬───┬───┐
│ 1 │ 2 │ 3 │ A │
├───┼───┼───┼───┤
│ 4 │ 5 │ 6 │ B │
├───┼───┼───┼───┤
│ 7 │ 8 │ 9 │ C │
├───┼───┼───┼───┤
│ * │ 0 │ # │ D │
└───┴───┴───┴───┘

*  = CLEAR (очистка)
#  = ENTER (подтверждение)
D  = PROGRAMMING MODE (программирование)
```

#### LED индикаторы

| Назначение | Arduino Pin | Цвет | Функция |
|------------|-------------|------|---------|
| LED Green | 12 | Зелёный | Доступ разрешён |
| LED Red | 13 | Красный | Доступ запрещён |
| LED Programming | 3 | Любой | Режим программирования |

#### I2C шина (LCD)

| Назначение | Arduino Pin | Описание |
|------------|-------------|----------|
| SDA | A4 | Data линия I2C |
| SCL | A5 | Clock линия I2C |
| VCC | 5V | Питание LCD модуля |
| GND | GND | Земля |

**I2C адрес LCD:** 0x27 (по умолчанию для большинства модулей)

#### Serial (отладка)

| Назначение | Arduino Pin | Скорость |
|------------|-------------|----------|
| TX | 1 | 9600 baud |
| RX | 0 | 9600 baud |

---

## Описание модулей

### 1. Модуль `main.cpp`

**Назначение:** Точка входа приложения, инициализация всех компонентов и главный цикл

**Основные функции:**

#### `void setup()`

**Последовательность инициализации:**

1. **Инициализация Serial (9600 baud)**
   - Перенаправление `stdout` и `stdin` на Serial
   - Вывод заголовка "Electronic Lock System v1.0"

2. **Тест LED (шаг 1/4)**
   - Инициализация зелёного и красного LED
   - Быстрое мигание для проверки работоспособности

3. **Инициализация клавиатуры (шаг 2/4)**
   - Настройка 8 пинов (4 строки OUTPUT, 4 столбца INPUT_PULLUP)

4. **Инициализация I2C шины (шаг 3/4)**
   - `Wire.begin()` для запуска I2C
   - Задержка 500мс для стабилизации

5. **Сканирование I2C устройств (шаг 4/4)**
   - Проверка всех адресов от 0x01 до 0x7E
   - Поиск LCD дисплея
   - Вывод найденных устройств с адресами

6. **Инициализация LCD и App**
   - Создание объекта `LcdI2c` с адресом 0x27
   - Создание объекта `App` со всеми компонентами
   - Вызов `app_ptr->begin()` для запуска приложения

7. **Финальный тест**
   - Тройное мигание зелёным LED
   - Вывод сообщения "System Ready!"

**Вывод в Serial при старте:**
```
=====================================
Electronic Lock System v1.0
=====================================
Initializing peripherals...

[1/4] Testing LEDs...
  LEDs: OK
[2/4] Testing Keypad...
  Keypad: OK (awaiting input)
[3/4] Initializing I2C...
  I2C Bus: OK
[4/4] Scanning I2C devices...
  -> Found device at 0x27
  Found 1 device(s) on I2C

Setting up LCD at 0x27...
Initializing application...
[LCD] Initialized with backlight ON
[APP] Welcome screen ready

=====================================
System Ready!
Code: 1234
*=Clear, #=Enter
=====================================

Testing LED blink...
Running system loop...
```

#### `void loop()`

**Главный цикл программы:**

```cpp
void loop()
{
  if (app_ptr != nullptr)
  {
    app_ptr->run();
  }
  delay(10);
}
```

- Вызывает `app_ptr->run()` каждые 10мс
- Простой и чистый — вся логика в классе `App`

---

### 2. Модуль `App` (Контроллер приложения)

**Файлы:** `app.h`, `app.cpp`

**Назначение:** Центральный контроллер, координирует работу всех компонентов

**Подробная документация:** См. [APP_FUNCTIONS_EXPLANATION.md](src/modules/app/APP_FUNCTIONS_EXPLANATION.md)

**Ключевые особенности:**

- Управление состояниями (обычный режим / режим программирования)
- Обработка ввода с клавиатуры
- Проверка пароля
- Визуальная и звуковая индикация
- Динамическое изменение пароля

**Константы:**
- `CODE_LENGTH = 4` — длина пароля
- `CLEAR_KEY = '*'` — очистка ввода
- `ENTER_KEY = '#'` — подтверждение
- `PROGRAM_KEY = 'D'` — вход в режим программирования

**Поля класса:**
- `_correctCode[5]` — текущий пароль (по умолчанию "1234")
- `_inputCode[5]` — буфер для ввода пользователя
- `_programmingMode` — флаг режима программирования
- Ссылки на все компоненты (Keypad, LCD, LEDs)

---

### 3. Модуль `Keypad` (Клавиатура)

**Файлы:** `keypad.h`, `keypad.cpp`

**Назначение:** Управление матричной клавиатурой 4×4

#### Класс `Keypad`

**Публичные методы:**

##### `Keypad(const uint8_t *rowPins, const uint8_t *colPins, uint8_t rows, uint8_t cols)`

**Конструктор:** Принимает массивы пинов для строк и столбцов

**Параметры:**
- `rowPins` — указатель на массив пинов строк (OUTPUT)
- `colPins` — указатель на массив пинов столбцов (INPUT_PULLUP)
- `rows` — количество строк (4)
- `cols` — количество столбцов (4)

##### `void begin()`

**Назначение:** Инициализация пинов клавиатуры

**Как работает:**
1. Настраивает пины строк как OUTPUT и устанавливает HIGH
2. Настраивает пины столбцов как INPUT_PULLUP
3. В состоянии покоя все строки HIGH, все столбцы подтянуты к HIGH через внутренние резисторы

##### `char getKey()`

**Назначение:** Чтение нажатой клавиши (блокирующий вызов)

**Алгоритм сканирования:**
```
Для каждой строки (row = 0..3):
    1. Установить строку в LOW
    2. Подождать 100 микросекунд
    3. Для каждого столбца (col = 0..3):
        a. Прочитать состояние столбца
        b. Если LOW → клавиша нажата!
        c. Debounce: подождать 20мс и проверить снова
        d. Если всё ещё LOW:
            - Получить символ из матрицы _keys[row][col]
            - Ждать отпускания (пока столбец не станет HIGH)
            - Debounce на отпускание: 50мс
            - Вернуть символ
    4. Установить строку обратно в HIGH
    
Если ни одна клавиша не нажата → вернуть '\0'
```

**Матрица символов:**
```cpp
const char _keys[4][4] = {
    {'1', '2', '3', 'A'},
    {'4', '5', '6', 'B'},
    {'7', '8', '9', 'C'},
    {'*', '0', '#', 'D'}
};
```

**Debouncing:**
- При нажатии: 20мс delay + повторная проверка
- При отпускании: 50мс delay
- Предотвращает ложные срабатывания от дребезга контактов

##### `bool keyPressed()`

**Назначение:** Быстрая проверка, нажата ли какая-то клавиша (не блокирующий)

**Возвращает:**
- `true` — хотя бы одна клавиша нажата
- `false` — ничего не нажато

**Примечание:** В текущей версии приложения этот метод не используется, но доступен для расширений.

---

### 4. Модуль `LcdI2c` (LCD дисплей)

**Файлы:** `lcd.h`, `lcd.cpp`

**Назначение:** Обёртка для работы с LCD 16×2 через I2C

**Зависимость:** Библиотека `LiquidCrystal_I2C`

#### Класс `LcdI2c`

**Публичные методы:**

##### `LcdI2c(uint8_t address = 0x27, uint8_t cols = 16, uint8_t rows = 2)`

**Конструктор:** Создаёт объект LCD

**Параметры:**
- `address` — I2C адрес дисплея (по умолчанию 0x27)
- `cols` — количество колонок (обычно 16)
- `rows` — количество строк (обычно 2)

##### `void begin()`

**Назначение:** Инициализация LCD дисплея

**Последовательность:**
1. Задержка 500мс для стабилизации I2C
2. Вызов `_lcd.init()` — инициализация дисплея
3. Включение подсветки `_lcd.backlight()`
4. Очистка дисплея `_lcd.clear()`
5. Установка курсора в позицию (0, 0)
6. Вывод подтверждения в Serial: "[LCD] Initialized with backlight ON"

**Задержки:** Между операциями добавлены задержки для надёжной работы I2C

##### `void print(const char *text)`

**Назначение:** Вывод текста на текущей позиции курсора

**Параметры:**
- `text` — строка для вывода

##### `void println(const char *text)`

**Назначение:** Вывод текста и переход на следующую строку

**Как работает:**
1. Выводит текст через `print(text)`
2. Перемещает курсор в начало второй строки `setCursor(0, 1)`

##### `void setCursor(uint8_t col, uint8_t row)`

**Назначение:** Установка позиции курсора

**Параметры:**
- `col` — колонка (0-15)
- `row` — строка (0-1)

**Защита:** Автоматически ограничивает значения в пределах размера дисплея

##### `void clear()`

**Назначение:** Очистка всего дисплея

**Задержка:** 2мс после очистки для завершения операции

##### `void write(uint8_t value)`

**Назначение:** Вывод одного символа по коду ASCII

**Использование:** Для вывода специальных символов, например `'*'` при вводе пароля

##### `void backlight()` / `void noBacklight()`

**Назначение:** Управление подсветкой дисплея

---

### 5. Модуль `Led` (Светодиод)

**Файлы:** `led.h`, `led.cpp`

**Назначение:** Управление одним светодиодом

#### Класс `Led`

**Публичные методы:**

##### `Led(uint8_t pin)`

**Конструктор:** Создаёт объект LED

**Параметры:**
- `pin` — номер цифрового пина Arduino

**Инициализация:**
- `_pin = pin`
- `_state = false` (выключен)

##### `void begin()`

**Назначение:** Инициализация пина

**Как работает:**
1. Настраивает пин как OUTPUT: `pinMode(_pin, OUTPUT)`
2. Устанавливает LOW: `digitalWrite(_pin, LOW)`
3. Сбрасывает состояние: `_state = false`

##### `void on()`

**Назначение:** Включение LED

**Как работает:**
1. `digitalWrite(_pin, HIGH)` — подача HIGH на пин
2. `_state = true` — обновление внутреннего состояния

##### `void off()`

**Назначение:** Выключение LED

**Как работает:**
1. `digitalWrite(_pin, LOW)` — установка LOW на пин
2. `_state = false` — обновление внутреннего состояния

##### `void toggle()`

**Назначение:** Переключение состояния (включить/выключить)

**Логика:**
```cpp
if (_state)
    off();
else
    on();
```

##### `bool state() const`

**Назначение:** Получение текущего состояния

**Возвращает:**
- `true` — LED включен
- `false` — LED выключен

**Использование в App:** Для отладки и логирования состояния LED

---

### 6. Модуль `SerialStdio` (Serial ввод/вывод)

**Файлы:** `serial_stdio.h`, `serial_stdio.cpp`

**Назначение:** Перенаправление стандартных потоков C (stdin/stdout) на Serial порт Arduino

**Преимущество:** Позволяет использовать функции `printf()`, `getchar()` вместо `Serial.print()`

#### Класс `SerialStdio`

**Статические методы:**

##### `static void begin(unsigned long baudRate)`

**Назначение:** Инициализация Serial с перенаправлением stdio

**Параметры:**
- `baudRate` — скорость передачи (обычно 9600)

**Как работает:**
1. Запускает Serial: `Serial.begin(baudRate)`
2. Настраивает файловые потоки:
   - `serial_stdout` → вывод на Serial через `serialPutchar()`
   - `serial_stdin` → чтение из Serial через `serialGetchar()`
3. Перенаправляет:
   - `stdout = &serial_stdout`
   - `stdin = &serial_stdin`

**Результат:** Теперь можно использовать `printf()` и `getchar()` для работы с Serial!

##### `static void printWelcome()`

**Назначение:** Вывод приветственного сообщения с инструкциями

**Вывод:**
```
STDIO serial ready. Commands: 'led1 on', 'led1 off', 'led2 on', 'led2 off'
                           'led both on', 'led both off'
```

**Примечание:** В текущей версии не используется, так как команды LED управляются через клавиатуру, а не Serial.

##### `static int readLine(char* buffer, int bufferSize)`

**Назначение:** Чтение строки из Serial до Enter

**Параметры:**
- `buffer` — буфер для хранения строки
- `bufferSize` — размер буфера

**Возвращает:** Количество прочитанных символов

**Поддерживаемые символы:**
- Печатные символы (ASCII 32-126) → добавляются в буфер
- `\r` или `\n` → завершение ввода
- `\b` или `127` (Backspace) → удаление последнего символа

**Примечание:** В текущей версии приложения не используется.

##### `static int serialPutchar(char c, FILE* stream)` (приватный)

**Назначение:** Callback для вывода одного символа в Serial

**Особенность:** Автоматически добавляет `\r` перед `\n` для совместимости с терминалами

##### `static int serialGetchar(FILE* stream)` (приватный)

**Назначение:** Callback для чтения одного символа из Serial

**Особенность:** Обрабатывает Backspace с выводом `\b \b` для визуального удаления символа

---

### 7. Модуль `CommandParser` (Парсер команд)

**Файлы:** `command.h`, `command.cpp`

**Назначение:** Парсинг текстовых команд для управления LED

**Статус:** Модуль включён в проект, но **не используется** в текущей версии. Предназначен для управления LED через Serial команды.

#### Enum `CommandType`

```cpp
enum CommandType {
    CMD_LED1_ON,      // "led1 on"
    CMD_LED1_OFF,     // "led1 off"
    CMD_LED2_ON,      // "led2 on"
    CMD_LED2_OFF,     // "led2 off"
    CMD_BOTH_ON,      // "led both on"
    CMD_BOTH_OFF,     // "led both off"
    CMD_UNKNOWN,      // Неизвестная команда
    CMD_EMPTY         // Пустая строка
};
```

#### Класс `CommandParser`

**Статические методы:**

##### `static CommandType parse(const char* input)`

**Назначение:** Парсинг строки в тип команды

**Параметры:**
- `input` — строка команды

**Обработка:**
1. Проверка на `nullptr` или пустую строку → `CMD_EMPTY`
2. Копирование в локальный буфер (максимум 32 символа)
3. Удаление пробелов в начале и конце (`trimWhitespace()`)
4. Преобразование в нижний регистр (`toLowerCase()`)
5. Сравнение со списком известных команд
6. Возврат соответствующего `CommandType` или `CMD_UNKNOWN`

**Поддерживаемые команды:**
- `"led1 on"` → `CMD_LED1_ON`
- `"led1 off"` → `CMD_LED1_OFF`
- `"led2 on"` → `CMD_LED2_ON`
- `"led2 off"` → `CMD_LED2_OFF`
- `"led both on"` → `CMD_BOTH_ON`
- `"led both off"` → `CMD_BOTH_OFF`

##### `static bool isValid(CommandType cmd)`

**Назначение:** Проверка, является ли команда валидной

**Возвращает:**
- `true` — команда валидна или пустая
- `false` — неизвестная команда

##### `static const char* toString(CommandType cmd)`

**Назначение:** Преобразование типа команды в строку

**Возвращает:** Текстовое представление команды

**Использование:** Для отладки и логирования

---

### 8. Модуль `I2CScanner` (Утилита)

**Файл:** `utils/i2c_scanner.h`

**Назначение:** Сканирование I2C шины для поиска подключённых устройств

#### Класс `I2CScanner`

**Статический метод:**

##### `static void scan()`

**Назначение:** Поиск всех I2C устройств на шине

**Алгоритм:**
```
Для адресов от 0x01 до 0x7E:
    1. Попытка начать передачу: Wire.beginTransmission(address)
    2. Завершение передачи: error = Wire.endTransmission()
    3. Анализ кода ошибки:
        - error == 0 → устройство найдено!
        - error == 4 → неизвестная ошибка
        - другое → устройство отсутствует
    4. Вывод адреса найденного устройства
```

**Вывод в Serial:**
```
Scanning I2C devices...
I2C device found at address 0x27
Total devices found: 1
```

**Типичные I2C адреса:**
- `0x27` или `0x3F` — LCD дисплей с I2C модулем
- `0x50` — EEPROM память
- `0x68` — RTC часы реального времени

**Использование:** В `main.cpp` вызывается при инициализации для диагностики подключения LCD

---

## Последовательность работы

### Диаграмма состояний

```
        ┌───────────────┐
        │   STARTUP     │
        │   setup()     │
        └───────┬───────┘
                │
                v
        ┌───────────────┐
   ┌───>│    WELCOME    │<────┐
   │    │  Enter Code:  │     │
   │    │    [    ]     │     │
   │    └───────┬───────┘     │
   │            │              │
   │            v              │
   │    ┌───────────────┐     │
   │    │  WAIT INPUT   │     │
   │    │  loop()->run()│     │
   │    └───────┬───────┘     │
   │            │              │
   │        [Key Press]        │
   │            │              │
   │      ┌─────┴──────┐      │
   │      │            │      │
   │      v            v      │
   │   [Digit]      [D] ?────>│ PROGRAMMING
   │      │         (D key)   │     MODE
   │      v                   │      │
   │  [* or #]                │      │
   │      │                   │      v
   │  ┌───┴────┐              │  [Input new
   │  │        │              │    4 digits]
   │  v        v              │      │
   │ [*]      [#]             │      v
   │Clear    Verify           │  [# confirm]
   │  │        │              │      │
   │  │   ┌────┴────┐         │      │
   │  │   │         │         │      │
   │  │   v         v         │      v
   │  │ Valid    Invalid      │  Save & Exit
   │  │   │         │         │      │
   │  │   v         v         │      │
   │  │ GREEN     RED         │      │
   │  │  LED      LED         │      │
   │  │ 5 sec    Blink×5      │      │
   │  │   │         │         │      │
   │  └───┴─────────┴─────────┴──────┘
   │              │
   └──────────────┘
```

### Подробное описание потока выполнения

#### 1. Запуск системы (Startup)

**Выполняется один раз при включении:**

```
1. setup() начало
2. Инициализация Serial (9600 baud)
3. Вывод заголовка "Electronic Lock System v1.0"
4. Тест LED (зелёный, красный)
5. Инициализация клавиатуры
6. Запуск I2C шины
7. Сканирование I2C устройств
8. Инициализация LCD
9. Создание объекта App
10. Вызов app.begin()
11. Отображение Welcome экрана
12. Тест: мигание зелёным LED 3 раза
13. Вывод "System Ready!"
14. Переход в loop()
```

#### 2. Главный цикл (Main Loop)

**Повторяется бесконечно:**

```
loop():
    if (app_ptr != nullptr):
        app_ptr->run()
    delay(10ms)
```

**`App::run()` делает следующее:**
```cpp
char key = keypad.getKey()
if (key != '\0'):
    if (programmingMode):
        processProgrammingInput(key)
    else:
        processInput(key)
```

#### 3. Обычный режим работы

**Сценарий A: Успешный вход**

```
1. Экран: "Enter Code: [    ]"
2. Пользователь нажимает: 1, 2, 3, 4
   - После каждой цифры: экран показывает [*   ], [**  ], [*** ], [****]
3. Пользователь нажимает: #
4. App проверяет: strcmp("1234", "1234") == 0 → SUCCESS!
5. Экран: "ACCESS GRANTED! Door Unlocked"
6. Зелёный LED включается на 5 секунд
7. Задержка 3 секунды после 5 секунд LED
8. Возврат на Welcome экран
```

**Сценарий B: Неудачный вход**

```
1. Экран: "Enter Code: [    ]"
2. Пользователь нажимает: 9, 9, 9, 9
   - Экран показывает: [****]
3. Пользователь нажимает: #
4. App проверяет: strcmp("9999", "1234") != 0 → FAILURE!
5. Экран: "ACCESS DENIED! Wrong Code"
6. Красный LED мигает 5 раз (150ms on, 150ms off)
7. Задержка 3 секунды
8. Возврат на Welcome экран
```

**Сценарий C: Очистка ввода**

```
1. Экран: "Enter Code: [    ]"
2. Пользователь нажимает: 1, 2
   - Экран: [**  ]
3. Пользователь нажимает: *
4. App очищает буфер
5. Возврат на Welcome экран: [    ]
```

#### 4. Режим программирования

**Сценарий D: Изменение пароля**

```
1. Welcome экран: "Enter Code: [    ]"
2. Пользователь нажимает: D
3. App переходит в режим программирования:
   - Экран: "PROG MODE | New Pass [    ]"
   - LED программирования (pin 3) включается
4. Пользователь вводит новый пароль: 5, 6, 7, 8
   - Экран: [*   ], [**  ], [*** ], [****]
5. Пользователь нажимает: #
6. App сохраняет новый пароль:
   - strcpy(_correctCode, "5678")
   - Экран: "PASSWORD SAVED | New: 5678"
7. Визуальное подтверждение:
   - Зелёный LED мигает дважды (300ms on, 200ms off, 300ms on)
8. LED программирования выключается
9. Задержка 2 секунды
10. Возврат на Welcome экран
11. Теперь для входа нужен код "5678"!
```

**Сценарий E: Отмена программирования**

```
1. Welcome экран
2. Пользователь нажимает: D
3. Режим программирования активен
   - Экран: "PROG MODE | New Pass [    ]"
4. Пользователь нажимает: *
5. App отменяет программирование:
   - programmingMode = false
   - LED программирования выключается
6. Возврат на Welcome экран
7. Пароль остаётся прежним
```

---

## Схема подключения

### Электрическая схема (текстовая)

```
           ┌─────────────────────────────────────┐
           │        ARDUINO UNO                  │
           │                                     │
           │  D0 (RX) ─────────┐                │
           │  D1 (TX) ─────────┤ Serial (USB)   │
           │                   │                 │
           │  D3  ──────────── LED Programming   │
┌──────────┤  D4  ─┐                            │
│ Keypad   ├  D5  ─┤                            │
│ 4×4      ├  D6  ─┤ Columns                    │
│ Matrix   ├  D7  ─┘                            │
│          ├  D8  ─┐                            │
│          ├  D9  ─┤                            │
│          ├  D10 ─┤ Rows                       │
└──────────┤  D11 ─┘                            │
           │  D12 ──────────── LED Green        │
           │  D13 ──────────── LED Red          │
           │                                     │
LCD I2C ───┤  A4 (SDA) ─┐                       │
16×2    ───┤  A5 (SCL) ─┤ I2C Bus              │
0x27       │            │                       │
           │  5V  ───────┬──── LCD VCC          │
           │  GND ───────┴──── LCD GND          │
           │                                     │
           └─────────────────────────────────────┘
```

### Схема подключения клавиатуры

**Клавиатура 4×4 (8 проводов):**

```
Keypad Module          Arduino Uno
─────────────          ───────────
    R1        ───────>    D8
    R2        ───────>    D9
    R3        ───────>    D10
    R4        ───────>    D11
    C1        ───────>    D4
    C2        ───────>    D5
    C3        ───────>    D6
    C4        ───────>    D7
```

### Схема подключения LCD дисплея

**LCD 16×2 с I2C модулем:**

```
LCD I2C Module         Arduino Uno
──────────────         ───────────
    GND       ───────>    GND
    VCC       ───────>    5V
    SDA       ───────>    A4
    SCL       ───────>    A5
```

**Примечание:** I2C модуль для LCD обычно имеет встроенный потенциометр для регулировки контрастности.

### Схема подключения LED

**3 светодиода с резисторами:**

```
LED Green:
    Arduino D12 ──> [Резистор 220Ω] ──> LED+ ──> LED- ──> GND

LED Red:
    Arduino D13 ──> [Резистор 220Ω] ──> LED+ ──> LED- ──> GND

LED Programming:
    Arduino D3  ──> [Резистор 220Ω] ──> LED+ ──> LED- ──> GND
```

**Важно:** Всегда используйте токоограничивающие резисторы (220Ω - 1kΩ) для защиты LED!

---

## Компиляция и загрузка

### Требования

**Программное обеспечение:**
- [PlatformIO](https://platformio.org/) (рекомендуется) или Arduino IDE
- Драйверы для Arduino Uno (обычно устанавливаются автоматически)

**Аппаратное обеспечение:**
- Arduino Uno (или совместимая плата)
- USB кабель

### Компиляция проекта

#### Через PlatformIO (рекомендуется)

**Используя VS Code:**

1. Откройте папку проекта в VS Code
2. Убедитесь, что установлено расширение PlatformIO IDE
3. Выберите нужную задачу:
   - **Build:** `platformio run`
   - **Upload:** `platformio run --target upload`
   - **Monitor:** `platformio device monitor`
   - **Clean:** `platformio run --target clean`

**Через терминал:**

```bash
# Перейти в папку проекта
cd /path/to/ES

# Компиляция
platformio run

# Загрузка на Arduino
platformio run --target upload

# Открыть Serial монитор
platformio device monitor

# Компиляция + загрузка + монитор
platformio run --target upload && platformio device monitor
```

#### Через Arduino IDE

**Если нужно использовать Arduino IDE:**

1. Установите библиотеку `LiquidCrystal_I2C` через Library Manager
2. Создайте новый проект (sketch)
3. Скопируйте все `.cpp` и `.h` файлы в папку проекта
4. Откройте `main.cpp` и переименуйте в `main.ino`
5. Выберите плату: `Tools > Board > Arduino Uno`
6. Выберите порт: `Tools > Port > COMX` (Windows) или `/dev/ttyUSB0` (Linux)
7. Нажмите `Upload`

### Конфигурация PlatformIO

**Файл `platformio.ini`:**

```ini
[env:uno]
platform = atmelavr
board = uno
framework = arduino
build_flags = 
    -I src/modules
    -I src/modules/serial_stdio
    -I src/modules/command
    -I src/modules/led
    -I src/modules/keypad
    -I src/modules/lcd
    -I src/modules/app
    -I src/modules/utils
lib_deps = 
    Wire
    LiquidCrystal_I2C
```

**Параметры:**
- `platform = atmelavr` — платформа для Arduino (ATmega328P)
- `board = uno` — Arduino Uno
- `framework = arduino` — использование Arduino фреймворка
- `build_flags` — пути для include файлов (для модульности)
- `lib_deps` — зависимости:
  - `Wire` — встроенная библиотека I2C
  - `LiquidCrystal_I2C` — библиотека для LCD

---

## Диагностика и отладка

### Serial Monitor

**Подключение:**
```bash
platformio device monitor
```

**Или через Arduino IDE:** `Tools > Serial Monitor` (9600 baud)

**Настройки:**
- Baud rate: 9600
- Line ending: Both NL & CR

### Типичные сообщения в Serial

**При запуске:**
```
=====================================
Electronic Lock System v1.0
=====================================
Initializing peripherals...

[1/4] Testing LEDs...
  LEDs: OK
[2/4] Testing Keypad...
  Keypad: OK (awaiting input)
[3/4] Initializing I2C...
  I2C Bus: OK
[4/4] Scanning I2C devices...
  -> Found device at 0x27
  Found 1 device(s) on I2C

[LCD] Initialized with backlight ON
[APP] Welcome screen ready

=====================================
System Ready!
Code: 1234
*=Clear, #=Enter
=====================================
```

**При нажатии клавиш (обычный режим):**
```
[KEYPAD] 1
[KEYPAD] 2
[KEYPAD] 3
[KEYPAD] 4
[KEYPAD] #
[CODE] 1234
[SUCCESS] CODE VALID! UNLOCKING!
[DEBUG] >>> TURNING ON GREEN LED <<<
[DEBUG] Before ON - LED state: 0
[DEBUG] After ON - LED state: 1
[DEBUG] LED should be ON for 5 seconds...
[DEBUG] Turning OFF LED
```

**При программировании:**
```
[KEYPAD] D
[PROGRAMMING] Entered programming mode
[PROGRAMMING] Turning ON LED on pin 3
[PROG-INPUT] 5
[PROG-INPUT] 6
[PROG-INPUT] 7
[PROG-INPUT] 8
[PROG-INPUT] #
[PROGRAMMING] New password set to: 5678
[PROGRAMMING] Turning OFF LED - exiting programming mode
```

### Решение проблем

#### Проблема 1: LCD не отображает текст

**Симптомы:**
- Подсветка горит, но текста нет
- Или LCD полностью не работает

**Диагностика:**
1. Проверьте подключение I2C (SDA → A4, SCL → A5)
2. Проверьте питание LCD (5V и GND)
3. Проверьте вывод Serial: должно быть "Found device at 0x27"
4. Если устройство не найдено:
   ```
   WARNING: No I2C devices found!
   Verify connections:
     SDA -> A4 | SCL -> A5 | GND | VCC 5V
   ```

**Решение:**
1. Проверьте все соединения
2. Отрегулируйте контрастность на I2C модуле (потенциометр)
3. Попробуйте другой I2C адрес (0x3F вместо 0x27):
   ```cpp
   // В main.cpp
   static uint8_t LCD_I2C_ADDRESS = 0x3F;
   ```

#### Проблема 2: Клавиатура не реагирует

**Симптомы:**
- При нажатии клавиш ничего не происходит
- LCD показывает Welcome, но нет [*]

**Диагностика:**
1. Проверьте подключение всех 8 проводов клавиатуры
2. Проверьте Serial: должны быть сообщения `[KEYPAD] X`

**Решение:**
1. Проверьте распиновку:
   - Строки R1-R4 → D8-D11
   - Столбцы C1-C4 → D4-D7
2. Проверьте правильность порядка проводов (часто перепутаны)
3. Попробуйте нажать каждую клавишу и проверить Serial

#### Проблема 3: LED не включаются

**Симптомы:**
- При правильном коде зелёный LED не горит
- При неправильном коде красный LED не мигает

**Диагностика:**
1. Проверьте Serial:
   ```
   [DEBUG] Before ON - LED state: 0
   [DEBUG] After ON - LED state: 1
   ```
2. Если state меняется, но LED не горит — проблема в подключении

**Решение:**
1. Проверьте полярность LED (длинная ножка = "+")
2. Проверьте наличие резистора (220Ω - 1kΩ)
3. Проверьте пины:
   - Зелёный → D12
   - Красный → D13
   - Программирование → D3
4. Попробуйте подключить LED напрямую (из теста при запуске)

#### Проблема 4: Компиляция не проходит

**Симптомы:**
```
fatal error: LiquidCrystal_I2C.h: No such file or directory
```

**Решение:**
1. Установите библиотеку `LiquidCrystal_I2C`:
   ```bash
   platformio lib install "LiquidCrystal I2C"
   ```
2. Или добавьте в `platformio.ini`:
   ```ini
   lib_deps = 
       marcoschwartz/LiquidCrystal_I2C@^1.1.2
   ```

#### Проблема 5: Символы на LCD отображаются некорректно

**Симптомы:**
- Квадратики вместо текста
- Случайные символы

**Решение:**
1. Добавьте задержки после операций с LCD (уже есть в коде)
2. Проверьте качество I2C соединения (короткие провода)
3. Добавьте pull-up резисторы на SDA и SCL (4.7kΩ к 5V)

### Расширение функционала

**Идеи для улучшения:**

1. **Сохранение пароля в EEPROM:**
   - Пароль сохраняется после перезагрузки
   - Используйте `EEPROM.h` библиотеку

2. **Счётчик неудачных попыток:**
   - Блокировка после 3 неудачных попыток
   - Таймаут 30 секунд

3. **Звуковая индикация:**
   - Добавить buzzer для звуковых сигналов
   - Разные мелодии для успеха/ошибки

4. **Поддержка нескольких пользователей:**
   - Массив паролей
   - Разные уровни доступа

5. **Реле для управления замком:**
   - Подключить реле к D13
   - Открытие реального электромагнитного замка

6. **RTC модуль:**
   - Логирование времени входа
   - Временные окна доступа

7. **SD карта:**
   - Запись логов на SD карту
   - История всех попыток входа

---

## Заключение

**Electronic Lock System v1.0** — это полнофункциональная система контроля доступа с:

✅ Модульной архитектурой  
✅ Надёжной обработкой ввода  
✅ Визуальной и аппаратной индикацией  
✅ Возможностью программирования пароля  
✅ Подробной диагностикой через Serial  

Система готова к использованию и легко расширяется для добавления новых функций.

---

**Авторские права:** © 2026  
**Лицензия:** Open Source  
**Платформа:** PlatformIO + Arduino Framework

---

*Документация создана для учебных и коммерческих целей*
